:80 {
    # Apunta a la carpeta pública de Laravel
    root * /var/www/html/public
    
    # Pasa las peticiones al procesador de PHP
    php_fastcgi 127.0.0.1:9000
    
    # Permite servir archivos estáticos (como las imágenes de storage)
    file_server
}